<%= form_for @response, :html => { :class => "form-horizontal response" } do |f| %>

  <% if @response.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@response.errors.count, "error") %> prohibited this response from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @response.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :email, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :email, :class => 'form-control' %>
    </div>
    <%=f.error_span(:email) %>
  </div>
  <div class="form-group">
    <%= f.label :contents, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :contents, :class => 'form-control' %>
    </div>
    <%=f.error_span(:contents) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                responses_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
